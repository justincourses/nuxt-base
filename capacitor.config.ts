import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.justincourse.nuxtbase',
  appName: 'nuxt-base-app',
  webDir: 'dist'
};

export default config;
